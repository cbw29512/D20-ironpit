[project]
name = "iron-pit-backend"
version = "0.1.0"
description = "Rules-first gladiator combat engine and certification oracle"
requires-python = ">=3.12"
dependencies = [
  "pydantic",
]

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]