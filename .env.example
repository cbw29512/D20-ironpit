# Optional settings for running the Python reference API locally.
# The production Netlify/browser fight path does not require environment variables
# and must not depend on IRON_PIT_API_BASE.

ALLOWED_ORIGINS=http://localhost:8080
LOG_LEVEL=INFO
