# Public URL of the deployed FastAPI service. This value is safe to expose in the browser.
IRON_PIT_API_BASE=https://your-api-host.example.com

# Comma-separated browser origins allowed to call FastAPI directly.
# For the public unauthenticated MVP, Render is configured with "*".
ALLOWED_ORIGINS=http://localhost:8080

LOG_LEVEL=INFO
